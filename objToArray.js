const usersObj = {
  name: "Ricarlison",
  age: 23,
  height: 1.74
};

// const usersArray = Object.keys(usersObj); //print keys
const usersArray = Object.values(usersObj); //print values
console.log(usersArray);
